# Nginx configuration for reverse proxy
events {}
http {
    server {
        listen 80;
        location /api/ { proxy_pass http://api:3001/; }
        location /ml/ { proxy_pass http://ml-api:8000/; }
        location / { proxy_pass http://web:3000/; }
    }
}